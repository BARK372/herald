server:
  host: "127.0.0.1"
  port: 8420
  log_level: "debug"

projects:
  herald:
    path: "/home/benjamin/Documents/kOlapsis/herald"
    description: "Herald - MCP Bridge for Claude Code"
    default: true
    allowed_tools:
      - "Read"
      - "Write"
      - "Edit"
      - "Bash(git *)"
      - "Bash(go *)"
      - "Bash(make *)"
    max_concurrent_tasks: 1
    git:
      auto_branch: true
      auto_stash: true
      branch_prefix: "herald/"
